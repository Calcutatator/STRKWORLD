/**
 * @strkworld/lobby — multiplayer presence.
 *
 * Broadcasts where avatars are. Nothing else. Never an address, balance,
 * transaction hash, or building name — see README.md.
 *
 * Implementation lands in Phase 1. See docs/SPEC.md §8.
 */

export {};
